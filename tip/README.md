# Tip Jar short alias

Canonical Tip Jar lives at [`/tip-jar/`](../tip-jar/).

This `/tip/` path is a trailing-slash alias so short URLs resolve on GitHub Pages (same pattern as `/pay/` and `/sales/`).

Zero entitlement · Stars gratitude only · NFA.
